

        GS_OPT(2)        Kreider OS-9 C Library        GS_OPT(2)


        NNAAMMEE 

             _gs_opt - get file descriptor options

        SSYYNNOOPPSSIISS 

             #include <sgstat.h>

             int _gs_opt(path, buffer)
             int path;
             struct sgbuf *buffer;

        DDEESSCCRRIIPPTTIIOONN 

             This function copies the options section of the path 
             descriptor opened on _p_a_t_h into the buffer pointer to by 
             _b_u_f_f_e_r.  The structure SSGGBBUUFF in SSGGSSTTAATT.HH provides a 
             convenient means to access the individual option values.  

             If _p_a_t_h is invalid, this function returns -1 as its value 
             and the error code is placed in the global variable _e_r_r_n_o.  

        NNOOTTEE 

             Be sure the buffer is large enough to hold all the 
             options.  Declaring the buffer as a type "struct sgbuf" is 
             perfectly safe as this structure is predefined to be large 
             enough for all the device descriptor options.  

        SSEEEE AALLSSOO 

             I$GetStt system call - Microware Manual, getstat(), _ss_opt().


























                                                                


