

        PUTS(3C)         Kreider OS-9 C Library         PUTS(3C)


        NNAAMMEE 

             puts, fputs - put a string on an output stream

        SSYYNNOOPPSSIISS 

             #include <stdio.h>

             puts(s)
             char *s;

             fputs(s, fp)
             char *s;
             FILE *fp;

        DDEESSCCRRIIPPTTIIOONN 

             The PPUUTTSS subroutine copies the null-terminated string _s to 
             the standard output stream _s_t_d_o_u_t and appends a new line 
             character.  

             The FFPPUUTTSS subroutine copies the null-terminated string _s to 
             the named output _f_p.  

             Neither routine copies the terminal null character.  

        CCAAVVEEAATTSS 

             The PPUUTTSS subroutine appends a new line, while FFPPUUTTSS does 
             not.  This inconsistency of the new-line being appended by 
             PPUUTTSS and not by FFPPUUTTSS is dictated by history and the desire 
             for compatibility.  

        SSEEEE AALLSSOO 

             fopen(), gets(), putc(), printf(), ferror() fread() 






















                                                                


