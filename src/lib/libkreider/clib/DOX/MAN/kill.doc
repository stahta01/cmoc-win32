

        KILL(3S)         Kreider OS-9 C Library         KILL(3S)


        NNAAMMEE 

             kill - send an interrupt to a process

        SSYYNNOOPPSSIISS 

             #include <signal.h>

             kill(tid, interrupt)
             int tid, interrupt;

        DDEESSCCRRIIPPTTIIOONN 

             KKIILLLL sseennddss tthhee ssiiggnnaall ttyyppee _i_n_t_e_r_r_u_p_t to the process with id 
             _t_i_d.  Both tasks, sender and receiver, must have the same 
             user ID unless the users or sender is the super user.  

             The include file contains definitions of the defined 
             signals as follows: 

                  #define  SIGKILL  0   /* system abort cannot be */
                                        /* caught or ignored */
                  
                  #define  SIGWAKE  1   /* wake up signal */
                  #define  SIGQUIT  2   /* keyboard abort signal */
                  #define  SIGINT   3   /* keyboard interrupt signal */

             Other user-define signals my also be sent.  

        DDIIAAGGNNOOSSTTIICCSS 

             KKIILLLL returns 0 from a successful call and -1 if the process 
             does not exist, the effective user IDs do not match, or the 
             user is not the super user.  

        SSEEEE AALLSSOO 

             signal(), OS-9 Shell command "kill"




















                                                                


