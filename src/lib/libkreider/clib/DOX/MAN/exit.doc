

        exit(3C)         Kreider OS-9 C Library         exit(3C)


        NNAAMMEE 

             exit - terminate a process after flushing any pending output

        SSYYNNOOPPSSIISS 

             exit(status)
             int status;

             _exit(status)
             int status;

        DDEESSCCRRIIPPTTIIOONN 

             EEXXIITT is the normal means of terminating a task.  The EEXXIITT 
             function terminates a process after calling the Standard 
             I/O library function __c_l_e_a_n_u_p, to flush any buffered 
             output.  

             The _eexxiitt function performs the same, but DOES NOT flush 
             any file buffers prior to exiting the task.  Neither the 
             EEXXIITT or _EEXXIITT functions ever return.  

             A process finishing normally, that is, returning from 
             _m_a_i_n(), is equivalent to a call to EEXXIITT.  

             The status passed to EEXXIITT is avaiable to the parent process 
             if it is executing a _w_a_i_t. _A_n _e_x_a_m_p_l_e _i_s: 

                  static int stat;
                  char *status = &stat;
                  .
                  .
                  os9fork (cmds, strlen(params), params, 1, 1, 0);
                  wait (status);
                  .
                  .


        SSEEEE AALLSSOO 

             wait()
















                                                                


