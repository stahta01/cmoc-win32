

        SS_REL(2)        Kreider OS-9 C Library        SS_REL(2)


        NNAAMMEE 

             _ss_rel - release a pending signal

        SSYYNNOOPPSSIISS 

             int _ss_rel(path)
             int path;

        DDEESSCCRRIIPPTTIIOONN 

             SSSS_RREELL cancels the signal to be sent from a device on data 
             ready.  The function SSSS_SSIIGG enables a device to send a 
             signal to a process when data is available on the device.  
             If an error occurs, the function returns the value -1 and 
             the appropriate error value is placed in the global 
             variable _e_r_r_n_o.  

        NNOOTTEE 

             The signal request is also canceled when the issuing 
             process dies or closes the path to the device.  This 
             feature exists only on SSCCFF devices.  

        SSEEEE AALLSSOO 

             I$SetStt system call, Microware manual, _ss_ssig().































                                                                


