

        GS_POS(2)        Kreider OS-9 C Library        GS_POS(2)


        NNAAMMEE 

             _gs_pos - get file position pointer

        SSYYNNOOPPSSIISS 

             long _gs_pos(path)
             int path;

        DDEESSCCRRIIPPTTIIOONN 

             This function returns the value of the file pointer for the 
             file opened on _p_a_t_h.  

             If _p_a_t_h is invalid, this function returns -1 as its value 
             and the error code is placed in the global variable _e_r_r_n_o.  

        NNOOTTEE 

             This call works only on RRBBFF devices.  It is unique to OS-9 
             and the only equivalent portable call is _l_s_e_e_k().  DO NOT 
             use this call if buffered I/O is being performed on the 
             path.  Use _f_t_e_l_l() instead.  

        SSEEEE AALLSSOO 

             I$GetStt system call - Microware Manual, lseek(), ftell()































                                                                


