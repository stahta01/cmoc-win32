
#ifndef _RMA_H
#define _RMA_H

char* bin_dec(int num, char* str);
char* bin_hex(int num, char* str);
char* bin_rom(int num, char* str);

int dec_bin(char* str);
unsigned hex_bin(char* str);

char* to_lowrs(char* str);
char* to_upprs(char* str);

#endif

